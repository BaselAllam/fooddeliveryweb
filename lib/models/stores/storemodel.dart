

class StoreModel{

  String id;
  String storeName;
  double distance;
  double rating;
  String img;
  bool isFav;
  String service;

  StoreModel(this.id, this.storeName, this.distance, this.rating, this.img, this.isFav, this.service);
}